import { DenemeDirective } from './deneme.directive';

describe('DenemeDirective', () => {
  it('should create an instance', () => {
    const directive = new DenemeDirective();
    expect(directive).toBeTruthy();
  });
});
